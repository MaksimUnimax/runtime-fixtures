# M4B1 R3 REACQUISITION — canonical ChatGPT Work execution prompt

WORK_ID: `OCTOPORT_SEO_M4B1_R3_REACQUISITION_2026-09-22_R1`

CONTINUE THE EXISTING OCTOPORT SEO PROGRAM.

THIS IS AN EXECUTION TASK.

THIS IS NOT A NEW PROJECT.
THIS IS NOT M4A.
THIS IS NOT M4B1 R1.
THIS IS NOT M4B1 R2.
THIS IS NOT M4B2.
THIS IS NOT M4Q.
THIS IS NOT M4C.

The original accepted M4B1 Coverage Closure R3 Work return was lost at the persistence handoff and is not durably present in GitHub.

The owner has explicitly authorized a FRESH REACQUISITION of the exact lost R3 bounded execution unit from the accepted R1+R2 authority.

This is NOT a byte-for-byte reconstruction of the 2026-09-18 snapshot.

It is a current public reacquisition of the same deterministic R3 unit under the current approved M4 method.

Repository:

`MaksimUnimax/runtime-fixtures`

Branch:

`seo/wordstat-batch-01-2026-09-16`

## 0. FIRST ACTION — NARROW PREFLIGHT

Fetch the live remote branch and record:

`START_HEAD`

Read in full:

- `docs/seo/LEVEL1/README.md`
- `docs/seo/EXECUTION_RULES.md`
- `docs/seo/WORK_HANDOFF_RULE.md`
- `docs/seo/QUALITY_FIRST_RESOURCE_RULE.md`
- `docs/seo/LEVEL2/README.md`
- `docs/seo/LEVEL2/OCTOPORT_STEP_RULES_INDEX.md`
- `docs/seo/LEVEL2/M4_SEARCH_COMPETITOR_LANDING_RULES.md`
- `docs/seo/serp/competitors/M4B1_R3_REACQUISITION_GATE_2026-09-22_R1.md`
- `docs/seo/serp/competitors/M4B1_R3_REACQUISITION_RELEASE_2026-09-22_R1.md`
- `docs/seo/serp/competitors/M4B1_R3_PERSISTENCE_INCIDENT_2026-09-22.md`
- `docs/seo/serp/competitors/M4B1_R2_MAIN_CHAT_MERGED_QA_2026-09-18.md`
- `docs/seo/serp/competitors/M4B_PAGE_SURFACE_FRONTIER_METHOD_CORRECTION_2026-09-18.md`
- `docs/seo/serp/competitors/M4_PROGRESS.md`

Do NOT redesign methodology.

Do NOT repeat Main Chat governance/research.

Do NOT infer authority from memory.

If any governing authority materially conflicts with this released recovery unit, return:

`HOLD_AUTHORITY_DRIFT`

and stop before acquisition.

## 1. VERIFY FROZEN PRE-R3 INPUTS

The accepted pre-R3 unit is frozen.

Verify exactly:

```text
AUTHORIZED_M4B1_ENTITIES = 45
ACCEPTED_M4A_ANCHORS = 100

R1_URL_ROWS = 372
R1_PAGE_EVIDENCE_ROWS = 249

R2_INITIAL_NAV_DELTA_ROWS = 835
R2_RECURSIVE_NEW_URL_IDENTITIES = 405
R2_PAGE_EVIDENCE_ROWS = 579

PRE_R3_CURRENT_NORMALIZED_UNIVERSE = 1612
R2_OPEN_URL_UNRESOLVED = 0
```

Verify these frozen blob identities:

```text
R1_SCOPE_BLOB = 3a0af7e2745d46d14634760b1e464a85c590b4c7
R1_URL_BLOB = a9b3f1bf9878e6008d82994e512c4e3fdb3c5165
R1_PAGE_BLOB = b24772cbd3b9d54b9ff300510f362ca7459cc883

R2_URL_OVERLAY_BLOB = 90d6dd1d483a8e6c78bcbab1033c39466b7714bb
R2_PAGE_OVERLAY_BLOB = c7b0a8774ed83088a00ea79dabc8f0d48a2db050
R2_ENTITY_CURRENT_BLOB = 860a1eca42054bb3b04a1c307da879ecf27d97ac
R2_FRONTIER_RECONCILIATION_BLOB = af9e801cdf3af66700fd122371a2187de748df5f

R2_MAIN_CHAT_QA_BLOB = dee0ef305301f2d81855fc72595654844b48a04a
FRONTIER_METHOD_CORRECTION_BLOB = 37aa4f3320ba54806d41a3c3c8d19892827d3d82
```

If a frozen input identity differs materially:

`HOLD_FROZEN_INPUT_DRIFT`

Do not continue.

## 2. LARGE-DATA RULE

Apply exactly:

```text
LARGE DATA != SAMPLE
LARGE DATA != FIRST-N
LARGE DATA != TRUNCATE
LARGE DATA != SUMMARY BEFORE FULL ANALYSIS

LARGE DATA
-> PROCESS THE COMPLETE BOUNDED EXECUTION UNIT IN CHATGPT WORK
```

Process ALL 45 authorized entities.

No representative subset.

No top-N shortcut.

No partial crawl presented as completion.

## 3. EXACT R3 EXECUTION UNIT

Reacquire only the lost M4B1 R3 page-surface closure layer.

For EVERY one of the 45 entities, terminally account for:

```text
ANCHORS
PRIMARY_NAV_TAXONOMY
BREADCRUMB_OR_LOCAL_SUBTREE
SCOPED_SITEMAP
PAGINATION_OR_LOAD_MORE
NEW_ELIGIBLE_URL_DELTA
```

Canonical invariant:

```text
COMPLETE_BOUNDED_FRONTIER != COMPLETE_INTERNAL_LINK_GRAPH
```

Do NOT replay the R2 416 child-link diagnostic pages as a mandatory queue.

Do NOT compute the transitive closure of every internal link.

Do NOT follow unrelated body links, footer links, recommendation widgets or generic corporate/media links merely because they are linked.

## 4. ANCHOR / NAVIGATION REUSE

All 100 accepted M4A anchors are already accounted by R1/R2.

Do NOT reacquire all anchors merely for ritual repetition.

Reuse accepted R1/R2 evidence where sufficient.

For navigation/taxonomy:
- reuse accepted navigation evidence where adequate;
- perform a narrow current public check only where needed to define the current bounded relevant surface.

Repeated sitewide navigation does not need to be re-enumerated from every page.

## 5. BREADCRUMB / LOCAL SUBTREE

For `EVIDENCE_ANCHORED_RELEVANT_SUBTREE` entities:

Use accepted anchors/current pages plus current public evidence only as needed to establish the bounded relevant subtree.

For `THEME_SCOPED_PUBLIC_TAXONOMY` entities:

If primary taxonomy already defines the relevant scope, breadcrumb-specific expansion may be `NOT_APPLICABLE` with explicit basis.

Do not expand through unrelated adjacent content.

## 6. SCOPED SITEMAP

For every entity:

1. inspect public `robots.txt` where available for sitemap declarations;
2. inspect declared sitemap/sitemap-index URLs;
3. if none is declared, check a conventional public sitemap location where reasonable;
4. enumerate sitemap indexes completely where accessible;
5. admit only entries deterministically inside the frozen authorized host/path/theme scope;
6. do not import unrelated whole-site URLs from broad global sitemaps;
7. record the exact terminal channel state and basis.

Allowed terminal states include:

`COMPLETE`
`NOT_APPLICABLE`
`NOT_FOUND`
`ACCESS_BLOCKED`
`UNSCOPABLE_WITH_REASON`
`HOLD`

Do not sample a large sitemap.

If it is large, process the complete bounded scoped unit.

## 7. PAGINATION / LOAD-MORE

For each admitted collection/category/blog/help/taxonomy surface:

- detect sequential page-number URLs;
- detect explicit next/prev controls;
- detect stable public load-more endpoints/URLs where legitimately observable;
- enumerate the complete bounded collection when possible;
- use `NOT_APPLICABLE` where no pagination exists;
- use `ACCESS_BLOCKED` or `HOLD` only when evidence supports it.

Do not treat related-post recommendations as pagination.

## 8. URL IDENTITY / TERMINALIZATION

Compare every R3-discovered URL against the frozen pre-R3 1612 normalized identities.

Preserve separately:

- raw discovered URL;
- discovery source/channel;
- normalized comparison identity;
- redirect chain;
- final URL;
- declared canonical where observable;
- scope disposition;
- terminal state;
- capture timestamp;
- capture method.

Allowed relation/disposition examples:

`ALREADY_CURRENT`
`NEW_IN_SCOPE`
`NEW_OUT_OF_SCOPE`
`DUPLICATE_CANONICAL`
`FACET_OR_SORT_VARIANT`
`NON_HTML`
plus evidence-backed inaccessible/error terminal states.

Every admitted new in-scope URL must reach a terminal state.

No silent URL loss.

## 9. PAGE EVIDENCE

For EVERY fresh R3 `INSPECTED` new identity create structured page evidence.

Use the existing M4B1 evidence schema.

Do not store full HTML or full-page text as deliverables.

Use structured observations and paraphrase.

Preserve at least:
- page/url/registry identity;
- final URL;
- capture timestamp;
- page role;
- title/meta/H1/heading/breadcrumb summary;
- marketplace scope;
- AI/LLM framing;
- user jobs;
- data scope;
- capability/action-boundary claims;
- integrations;
- pricing/availability;
- CTA;
- proof/trust;
- FAQ/subtopics where present;
- freshness/author cues;
- internal-link/taxonomy pattern;
- structured-data observation;
- evidence locator;
- confidence;
- Octoport overlap/difference;
- notes.

Competitor claims remain competitor evidence only.

## 10. CANDIDATE TERMS / TASKS

Create candidate rows only from newly inspected R3 pages.

Every candidate must retain exact page provenance.

Do not promote competitor wording/topic to proven demand.

Do not make final semantic cluster/page/URL/H1/Title/IA decisions.

Do not call Wordstat here.

## 11. CURRENT 45-ENTITY SYNTHESIS

Build a current 45-row synthesis using:

`R1 + R2 + fresh R3`.

Historical R1/R2 files remain immutable.

Do not silently rewrite accepted history.

## 12. FRONTIER RECONCILIATION

Build exactly 45 reconciliation rows.

For every entity preserve:

- R1 current URL count;
- R2 added URL count;
- fresh R3 discovered URL count;
- current merged unique normalized URL count;
- terminal current URL count;
- channel states;
- open URL unresolved;
- blocking channel HOLD;
- completion state.

The old R2 416 child-link count may be retained as diagnostic history only.

It cannot itself make the entity incomplete.

## 13. CURRENT VS HISTORICAL R3 COMPARISON

The old accepted R3 aggregate facts survived in later accepted authority:

```text
HISTORICAL_R3_NEW_IN_SCOPE_NORMALIZED_IDENTITIES = 2888
HISTORICAL_R3_NEW_INSPECTED_IDENTITIES = 2845
HISTORICAL_R3_EXECUTION_ENVIRONMENT_FAILURE_TERMINALS = 43
HISTORICAL_POST_R3_MERGED_NORMALIZED_UNIVERSE = 4500
HISTORICAL_OPEN_URL_UNRESOLVED = 0
HISTORICAL_SILENT_URL_LOSS = 0
```

These are COMPARATORS ONLY.

DO NOT force the fresh reacquisition to equal these numbers.

After the fresh current unit is complete, classify exactly one:

`REACQUIRED_EQUIVALENT_AT_AGGREGATE_LEVEL`

`CURRENT_PUBLIC_DRIFT_DETECTED`

`RECOVERY_INCOMPLETE_ENVIRONMENT_BLOCKED`

`AUTHORITY_DRIFT_HOLD`

If current public drift exists, preserve it as current evidence and produce exact dependency-impact notes for Main Chat.

Do NOT silently rewrite accepted R4/R5.

## 14. PUBLIC-ACCESS BOUNDARIES

Allowed:
- normal public web access inside the frozen 45-entity scope;
- public navigation/taxonomy;
- public robots.txt;
- public sitemap/sitemap indexes;
- public pagination/load-more;
- public new in-scope pages;
- redirect/canonical checks.

Forbidden:
- login;
- credential use not frozen in authority;
- CAPTCHA bypass;
- anti-bot bypass;
- paywall bypass;
- private APIs;
- cookie/session theft;
- Search provider calls;
- Wordstat calls;
- Alice/GenSearch calls;
- M4Q reverse-index calls;
- new competitors;
- whole-domain crawl;
- arbitrary sampling.

TOOL/ENVIRONMENT FAILURE != SITE FACT.

Classify environment uncertainty separately.

## 15. REQUIRED OUTPUTS — EXACTLY 9

Create exactly:

1. `M4B1R3_SOURCE_MANIFEST.md`
2. `M4B1R3_DISCOVERY_CHANNEL_COVERAGE.tsv`
3. `M4B1R3_URL_OVERLAY.tsv`
4. `M4B1R3_PAGE_EVIDENCE_OVERLAY.tsv`
5. `M4B1R3_CANDIDATE_TERMS_OVERLAY.tsv`
6. `M4B1R3_ENTITY_SYNTHESIS_CURRENT.tsv`
7. `M4B1R3_FRONTIER_RECONCILIATION.tsv`
8. `M4B1R3_QA.md`
9. `M4B1R3_RETURN_MANIFEST.json`

The return manifest must contain:
- WORK_ID;
- branch;
- START_HEAD;
- END_OBSERVED_HEAD;
- authority drift status;
- exact file list;
- SHA-256 for every non-self file;
- byte counts;
- row counts;
- all hard-QA counts;
- current-vs-historical classification.

## 16. HARD QA

Prove at minimum:

```text
LIVE_BRANCH_FETCHED = true
FROZEN_R1_R2_IDENTITIES_MATCH = true

AUTHORIZED_ENTITIES = 45
UNAUTHORIZED_ENTITIES = 0

DISCOVERY_CHANNEL_COVERAGE_ROWS = 45
ANCHOR_CHANNEL_TERMINAL = 45/45
PRIMARY_NAV_TAXONOMY_TERMINAL = 45/45
BREADCRUMB_LOCAL_CHANNEL_TERMINAL = 45/45
SITEMAP_CHANNEL_TERMINAL = 45/45
PAGINATION_CHANNEL_TERMINAL = 45/45

ALL_R3_NEW_ELIGIBLE_URLS_TERMINAL = true OR exact blockers returned
ALL_R3_NEW_INSPECTED_HAVE_PAGE_EVIDENCE = true
R3_NEW_URL_SILENT_LOSS = 0

FINAL_ENTITY_SYNTHESIS_ROWS = 45
FINAL_FRONTIER_RECONCILIATION_ROWS = 45

ARBITRARY_SAMPLING = 0
WHOLE_DOMAIN_CRAWL = 0
COMPLETE_INTERNAL_LINK_GRAPH_REQUIRED = false
R2_416_DIAGNOSTIC_REPLAY_REQUIRED = false

SEARCH_PROVIDER_CALLS = 0
WORDSTAT_CALLS = 0
ALICE_CALLS = 0
M4Q_CALLS = 0

NEW_COMPETITOR_ENTITIES = 0
FINAL_CLUSTER_PAGE_IA_DECISIONS = 0
COMPETITOR_CLAIM_AS_OCTOPORT_FACT = 0
COMPETITOR_TOPIC_AS_PROVEN_DEMAND = 0

HISTORICAL_R3_COMPARATOR_REPORTED = true
CURRENT_PUBLIC_DRIFT_CLASSIFIED = true

OPEN_CRITICAL_DEFECTS = 0 OR exact blockers returned
```

If complete closure is impossible, return:

`PARTIAL / RECOVERY_REQUIRED`

with exact entity/channel/URL residuals.

DO NOT fake PASS.

## 17. BEFORE RETURN

Re-fetch the remote branch and record:

`END_OBSERVED_HEAD`

If remote HEAD advanced since START_HEAD:

- classify changed paths;
- determine whether method/input authority changed;
- if material authority changed, return `HOLD_AUTHORITY_DRIFT`;
- if changes are non-material to this execution unit, report them explicitly and continue.

## 18. RETURN / HANDOFF

WORK MUST NOT WRITE TO GITHUB.

Create ONE ZIP containing EXACTLY the 9 final files.

Do not provide nine separate download links.

The final response MUST contain:

```text
WORK_ID =
START_HEAD =
END_OBSERVED_HEAD =
AUTHORITY_DRIFT_STATUS =
VERDICT =

AUTHORIZED_ENTITIES =
R3_NEW_URL_ROWS =
R3_NEW_INSPECTED_ROWS =
R3_BLOCKING_HOLD =
R3_OPEN_URL_UNRESOLVED =
R3_SILENT_URL_LOSS =

HISTORICAL_R3_COMPARISON =
FILES_IN_ZIP = 9
ZIP_SHA256 =
GITHUB_WRITES = 0
```

Then provide ONE REAL DIRECT/CLICKABLE DOWNLOADABLE LINK TO THE ZIP ARTIFACT.

A ChatGPT conversation URL is NOT a ZIP artifact link.

Owner will unpack and upload all 9 files together to:

`docs/seo/serp/competitors/work_return/M4B1_COVERAGE_CLOSURE_2026-09-18_R3/`

After owner upload Main Chat performs remote readback, hash/file QA and dependency reconciliation before M4C.
