# M4Q R2 targeted M4C recheck — source and observation manifest

WORK_ID: `OCTOPORT_SEO_M4Q_R2_TARGETED_M4C_RECHECK_2026-09-23_R1`  
START_HEAD: `bac03ea0c1fcbf517f6eee548750c62e9257d3ad`  
END_OBSERVED_HEAD: `bac03ea0c1fcbf517f6eee548750c62e9257d3ad`  
AUTHORITY_DRIFT_STATUS: `NONE`  
VERDICT: `PASS_TARGETED_RECHECK_WITH_TERMINAL_ACCESS_HOLDS`

## Authority and immutable scope

The live remote branch was fetched at start and immediately before return. Governing Level 1 and M4/M6 Level 2 rules, product truth, methodology, Pass B Main Chat acceptance, targeted recheck gate, M4C final acceptance and M4 progress were read from that branch. Their governing text had not changed since accepted Pass B. The binding target file `docs/seo/serp/competitors/M4Q_R2_TARGETED_M4C_RECHECK_URLS_2026-09-23_R1.tsv` has Git blob `c567aeef24441e2c0e3e05a153dd6bdf33384201`: 59 distinct IDs, 59 distinct exact ranking URLs, 12 authorized registry entities. Source Pass B matrix confirms 87 current top-20 occurrences on those 59 URL/entity pairs; accepted M4C URL ledger contains none of the exact 59 source URLs.

Accepted full-volume material read for reconciliation: the 60-row M4C hardened registry and coverage; all 15,469 accepted M4C URLs; all 8,431 candidate occurrences; all 5,973 M6 candidate groups; 1,850 Pass B observed competitor occurrences and the accepted eight-file Pass B return. The accepted `M4C_PAGE_EVIDENCE.tsv` was reconstructed from two published byte parts, verified at 34,206,423 bytes, SHA-256 `e0433463f29daf4d2bec1dccd3fec541e9263161672c752898be18c18e7fa175`, and read in full (12,502 rows). Source inputs are analytical authority; no repository file was changed by Work.

## Public-page acquisition and its limits

Exactly each of the 59 listed public URLs received one direct navigation attempt on 2026-09-23; no search-engine queries, provider calls, child-link traversal, host-variant substitutions or login attempts occurred. Pages were inspected from the rendered public DOM. For successful pages, the evidence table records title, exact H1, meaningful headings, breadcrumb if present, declared canonical, meta description where present, and **full captured visible body and main/article text** as reversibly escaped TSV fields. Each candidate is a verbatim H1 or rendered heading from its own target page, with its precise heading locator. In-scope H2/H3/H4 task headings were found by scanning all headings and retaining concrete seller tasks before related-article/footer sections; this is eligibility filtering across each full page, not first-N sampling. All 59 source URLs remain the only targets. No ordinary page link became a new fetch target.

The browser exposes the final rendered URL and public DOM but does **not** expose reliable HTTP response status or every intermediate HTTP redirect hop. Therefore `http_status_or_observation` explicitly says `HTTP_STATUS_NOT_EXPOSED_BY_BROWSER`, and `redirect_chain` distinguishes observed source/final identity from unavailable intermediate hops. A visible public page is `INSPECTED`, not an asserted HTTP 200. Six exact targets yielded navigation timeouts (five `mpmgr.ru`, one `wildcrm.ru`) and six `mpstats.io` targets displayed the literal resource-blocked message with a support ID. These are terminal `TIMEOUT` and `ROBOTS_OR_ACCESS_BLOCKED` observations, not fabricated 404/403 or a claimed CAPTCHA. No retries, bypasses, substitutes or deletion of accepted historical M4C evidence occurred.

## URL/page identity and reconciliation

M4C exact source URL absence is 59/59. For 8 Uniseller ranking URLs ending in `/`, the current page declares a canonical URL **without** the trailing slash that exactly matches an already accepted M4C URL/page identity. These eight are canonical alias links, never eight invented new independent pages. Their accepted M4C page evidence was compared against the current rendered title/H1: 2 have no material title/H1 change; 6 have fresh title/H1 evidence (in several historical captures the H1 was not extracted). Other accessible URLs are additive current URL/page observations for the same 12-entity authority. Three accessible pages have dominantly nonlaunch-scope content (1C documentflow, external bestseller/niche selection, or external competitor/niche intelligence) and remain explicit `OUT_OF_SCOPE` context.

The URL comparison used in the accepted Pass B is retained: lowercase URL scheme/authority, discard fragment only, preserve path spelling/query, normalize `/` versus empty **root** path only. A trailing slash on a nonroot path is **not** silently stripped; the eight aliases were admitted solely because each current page itself declared the exact accepted canonical URL. Candidate comparison uses only NFC, outer trim, internal whitespace collapse and Unicode case-fold. Different morphology or broad semantic themes are never silently merged. `ALREADY_PRESENT` requires that exact-safe raw wording exists in the accepted 8,431-row register; `POSSIBLE_VARIANT` is only a review flag, not semantic identity; `NEW_CANDIDATE` is newly observed competitor wording for later validation, not proven demand. Unsupported action, external intelligence, accounting and other irrelevant contexts remain out of scope or ambiguous.

TSV syntax: literal TAB field separator, physical newline record separator, quote literal (`QUOTE_NONE`). Embedded backslashes and control characters are reversibly escaped as `\\`, `\t`, `\r`, `\n`. Raw captured source body remains in the page evidence table. All accessible page content is current public competitor content only; it cannot authorize an Octoport feature, Wordstat demand, a final cluster, page owner or URL/H1/Title/IA decision.

The 74 accepted Pass B sibling-host identity holds remain unassigned and outside this recheck. M4Q known-query top-100 visibility retains its bounded 45-query claim; arbitrary competitor-domain to unknown-query reverse-index recall remains unproven.
