{
  "name": "@product/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsup src/main.ts --format esm --target node24 --clean"
  },
  "dependencies": {
    "@product/auth": "workspace:*",
    "@product/billing": "workspace:*",
    "@product/db": "workspace:*",
    "@product/device-auth": "workspace:*",
    "@product/email": "workspace:*",
    "@product/health": "workspace:*",
    "@product/observability": "workspace:*",
    "@product/shared": "workspace:*",
    "tsx": "4.20.5"
  },
  "devDependencies": {
    "tsup": "8.5.0"
  }
}
